# UploadThing dashboard → API Keys → copy token (server-only, never expose in client code)
UPLOADTHING_TOKEN=

# Optional: override API URL (defaults to /api/uploadthing via Vite proxy in dev)
# VITE_UPLOADTHING_API_URL=http://localhost:3001/api/uploadthing
